spring:
  cloud:
    aws:
      parameterstore:
        enabled: false

thumbsup:
  jwt:
    secret: test-secret-key-for-jwt-hmac-sha-256-minimum-32-bytes
  cors:
    allowed-origin-patterns:
      - http://localhost:3000
  swagger:
    basic-auth:
      username: swagger-test
      password: swagger-test-password
  elice:
    quiz:
      api-key: test-elice-api-key
      base-url: http://localhost:0/v1
      model: test-model
