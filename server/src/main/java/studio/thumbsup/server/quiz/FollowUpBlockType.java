package studio.thumbsup.server.quiz;

/**
 * 꼬리질문 "상세 정리" 블록의 종류.
 *
 * <p>지금은 텍스트뿐이지만 블록 배열 자체가 코드 예시·목록 같은 다른 표현을 받기 위한 구조다 —
 * 화면이 항목 수와 종류를 고정하지 않기 때문이다. 종류가 늘면 여기에 값을 추가한다.
 */
public enum FollowUpBlockType {
    TEXT
}
